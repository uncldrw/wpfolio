services:
  nginx:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./src/nginx/ssl:/etc/nginx/ssl:ro

  frontend:
    environment:
      NODE_ENV: production

  wordpress:
    environment:
      WORDPRESS_DEBUG: "0"
